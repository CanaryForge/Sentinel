\section{Reproducibility}

\todo{Comandos exactos, del mas barato al mas caro, copiados del README:
validacion de compose, dry-run, controles positivos, analisis, linea de
tiempo, barrido. Subrayar que todo salvo la matriz corre sin backend de LLM.}

\todo{Que datos se publican: las 63 corridas de \texttt{results/} y las 30 de
\texttt{results\_causal/} estan versionadas en el repositorio (~1.8 MB), asi
que cualquiera puede recomputar cada tabla de este paper sin gastar cuota.
Confirmar el commit exacto y el enlace al repositorio.}

\todo{Las tres trampas de entorno que conviene avisar antes de que alguien
pierda una tarde: CRLF en Windows, conversion de rutas de Git Bash, y el
limite de antiguedad de ingesta de OpenObserve. Todas documentadas en el
README.}

\subsection{Machine provenance}

\todo{Tabla resumida de \texttt{report/hardware\_validation.md}: que maquina
produjo que conjunto de datos. Incluir la advertencia de que la version de
Ollama del corpus principal sigue sin registrarse, y por que eso importa para
la Seccion 5.}
